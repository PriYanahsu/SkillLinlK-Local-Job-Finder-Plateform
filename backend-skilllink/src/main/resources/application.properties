# Application
spring.application.name=backend-skilllink

# Data Source Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/save_File_Data
spring.datasource.username=root
spring.datasource.password=Priyanshu@123

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Logging SQL Queries and Bind Parameters
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Optional: Hide stack trace noise (optional cleanup)
logging.level.org.hibernate.engine.internal.StatisticalLoggingSessionEventListener=OFF
